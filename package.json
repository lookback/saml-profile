{
  "name": "saml-profile",
  "version": "1.0.0",
  "description": "Given a saml response, converts the xml to a js object and logs the profile",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "xml2js": "^0.4.9"
  }
}
